{
  "name": "ppcc-bouncing-board",
  "version": "1.0.0",
  "description": "Bouncing board with synchronized leaderboard",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  }
}